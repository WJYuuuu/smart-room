; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples DUMPLOG
; https://docs.platformio.org/page/projectconf.html

[env:freenove_esp32_wrover]
platform = espressif32
board = freenove_esp32_wrover
framework = arduino
monitor_speed = 115200

; 添加所需库
lib_deps =
    marvinroger/AsyncMqttClient@^0.9.0 ; MQTT 异步客户端（高性能，推荐用于 ESP32）
    me-no-dev/ESPAsyncTCP@^1.2.2       ; 它依赖的底层网络库（通常自动安装，但显式写上更保险）
    me-no-dev/AsyncTCP @ ^1.1.1        ; 异步的TCP
    bblanchon/ArduinoJson@^7.0.4       ; 👈 添加这一行！
    adafruit/Adafruit GFX Library      ;
    adafruit/Adafruit SSD1306          ;
    4-20ma/ModbusMaster@^2.0.0         ; ModbusMaster
    knolleary/PubSubClient@^2.8        ; 同步的MQTT
; 可选：提升 LDF 模式以确保依赖被正确解析（非必须，但有时有用）
lib_ldf_mode = deep

board_build.filesystem = littlefs   ; 👈 关键！必须指定

build_flags =
    -Wno-deprecated-declarations        ; 消除 DynamicJsonDocument v6的警告


; ;OTA 配置 首次烧录要屏蔽该代码
; upload_protocol = espota
; upload_port = 192.168.0.170  ; ← 替换为你的 ESP32 IP

; ; 关键：提供 OTA 密码
; upload_flags =
;     --port=3232
;     --auth=12345678            ; ← 必须和 ArduinoOTA.setPassword() 一致

